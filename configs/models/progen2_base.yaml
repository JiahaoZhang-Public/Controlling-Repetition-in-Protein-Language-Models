defaults:
  - backend@backend: causal
  - _self_

name: progen2_base # 27 layers

params:
  model_name: hugohrban/progen2-base
  prompt_text: "<|endoftext|>"
  length_offset: 1
  tokenizer_kwargs:
    trust_remote_code: true
  model_kwargs:
    trust_remote_code: true
    torch_dtype: auto
  generation_kwargs:
    do_sample: true
    eos_token_id: 0
